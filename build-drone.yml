global-variables:
  environment: &default_environment
    REMOTE_URL: http://dsobot:pF:83b9:VFT9hV8@git:3000/DISION/new-dision.tech.git
    WORK_DIRECTORY: /data/drone/new-dision.tech
    LOCAL_SERVER_DIRECTORY: /data/shared/new-dision-tech

kind: pipeline
type: exec
name: step

clone:
  disable: true

steps:
  - name: Cleannn
    environment: *default_environment
    commands:
      - rm -rf $${WORK_DIRECTORY}
      - rm -rf $${LOCAL_SERVER_DIRECTORY}
  - name: Cloning
    environment: *default_environment
    commands:
      - git clone $${REMOTE_URL} $${WORK_DIRECTORY}